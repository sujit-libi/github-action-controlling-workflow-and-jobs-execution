name: Reusable Deployment
on: workflow_call # this allow to call from other workflow
  jobs:
    deploy:
      runs-on: ubuntu-latest
      steps:
        - name: Output Information
          run: echo "Deploying and uploading...."